@model IEnumerable<ibby_cms.Models.PageContentViewModel>
@{
    ViewBag.Title = "Admin panel";
}

<h2>Admin Panel</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Header)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Url)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Header)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Url)
            </td>
            <td>
                <div class="row">
                    <div>
                        @Html.ActionLink("Просмотреть", "ViewPage", new { id = item.Id })
                    </div>
                    <div>
                        @Html.ActionLink("MakePageContent", "MakePageContent", new { id = item.Id })
                    </div>
                    <div>
                        @Html.ActionLink("Удалить", "DeletePage", new { id = item.Id })
                    </div>
                </div>
            </td>
        </tr>
    }
</table>



<li>
    @Html.ActionLink("MakePageContent", "MakePageContent")
</li>
<li>
    @Html.ActionLink("Создать страницу", "CreatePage")
</li>
