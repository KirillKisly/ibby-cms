<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    <link href="~/Content/customMenu.css" rel="stylesheet">
</head>
@using Microsoft.AspNet.Identity
<body>
    @if (Request.IsAuthenticated) {
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" })) {
            @Html.AntiForgeryToken()

            <div class="navbar navbar-inverse navbar-fixed-top">

                @Html.ActionLink("ibby-cms", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })

                @Html.ActionLink("Вы зашли как " + User.Identity.GetUserName(), "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage", @class = "navbar-right navbar-brand" })



            </div>


            <div class="sidenav">

                <ul class="sidebar-nav">
                    <li class="sidebar-brand">@Html.ActionLink("Администрирование", "Index", "Admin")</li>
                    <li><br /></li>
                    <li>@Html.ActionLink("Управление страницами", "ManagementPage", "Admin")</li>
                    <li>@Html.ActionLink("Настройки", "")</li>
                    <li class="sidebar-brand"><hr /><a href="javascript:document.getElementById('logoutForm').submit()">Выйти</a></li>
                </ul>

            </div>

        }
    }

    <div class="main">
        @RenderBody()
        <hr />

        <footer>
            <p>&copy; @DateTime.Now.Year - ibby-cms Application</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)

</body>


</html>




@*<!DOCTYPE html>
    <html>
    <head>
        <title>@Page.Title</title>
        @RenderSection("head", required: false)

        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

        <title>Simple Sidebar - Start Bootstrap Template</title>
        <title>@ViewBag.Title - ibby-cms</title>
        @Styles.Render("~/Content/css")
        @Scripts.Render("~/bundles/modernizr")
        <!-- Bootstrap core CSS -->
        <link href="~/Content/bootstrap.min.css" rel="stylesheet">
        <!-- Custom styles for this template -->
        <link href="~/Content/CustomStyleMenu.css" rel="stylesheet">
    </head>

    <body>

        <div class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    @Html.ActionLink("ibby-cms", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
                </div>
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li>@Html.ActionLink("Главная", "Index", "Home")</li>
                        <li>@Html.ActionLink("О нас", "About", "Home")</li>
                        <li>@Html.ActionLink("Контакты", "Contact", "Home")</li>
                    </ul>
                    @Html.Partial("_LoginPartial")
                </div>
            </div>
        </div>
        <div class="container body-content">
            <div id="wrapper" class="toggled">
                <div id="sidebar-wrapper">
                    <ul class="sidebar-nav">
                        <li class="sidebar-brand">@Html.ActionLink("Администрирование", "Index", "Admin")</li>
                        <li></li>
                        <li>@Html.ActionLink("Управление страницами", "ManagementPage", "Admin")</li>
                    </ul>
                </div>

                <!-- Page Content -->
                <div id="page-content-wrapper">
                    <div class="container-fluid">
                        @RenderBody()
                    </div>
                    <hr />

                    <footer>
                        <p>&copy; @DateTime.Now.Year - ibby-cms Application</p>
                    </footer>
                </div>
            </div>
        </div>

        @Scripts.Render("~/bundles/jquery")
        @Scripts.Render("~/bundles/bootstrap")
        @RenderSection("scripts", required: false)
    </body>
    </html>
*@